# Data Folder

- `sample_flood_claims.csv` is synthetic demonstration data.
- `flood_claims_template.csv` contains the required header row for new local datasets.

Use local, minimal, non-sensitive identifiers where possible.
